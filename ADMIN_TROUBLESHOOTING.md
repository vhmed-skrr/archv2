# Admin Panel Troubleshooting Guide
## لوحة التحكم - دليل حل المشاكل

---

## ⚠️ المشكلة: الـ Admin لا يعمل

### الحلول المحتملة:

### 1. تفعيل Netlify Identity (الأهم!)

**المشكلة**: لم يتم تفعيل Netlify Identity في لوحة تحكم Netlify

**الحل**:
1. اذهب إلى Netlify Dashboard
2. اختر موقعك
3. **Site Settings** > **Identity**
4. اضغط **Enable Identity**
5. في نفس الصفحة، اذهب إلى **Services** > **Git Gateway**
6. اضغط **Enable Git Gateway**

---

### 2. التأكد من رفع الملفات

**تأكد من رفع هذه الملفات إلى Netlify:**
- ✅ `admin/index.html`
- ✅ `admin/config.yml`
- ✅ `netlify.toml`

**كيف تتأكد:**
```bash
git add admin/ netlify.toml
git commit -m "Add Decap CMS"
git push
```

---

### 3. مسح الـ Cache

**المشكلة**: المتصفح يستخدم نسخة قديمة

**الحل**:
1. افتح `/admin` في نافذة تصفح خفي (Incognito)
2. أو امسح الـ cache:
   - Chrome: `Ctrl + Shift + Delete`
   - اختر "Cached images and files"
   - امسح

---

### 4. التحقق من الأخطاء

**افتح Console في المتصفح:**
1. اضغط `F12`
2. اذهب إلى تبويب **Console**
3. ابحث عن أخطاء باللون الأحمر

**أخطاء شائعة:**
- `Failed to load config.yml` → تأكد من وجود الملف
- `Identity not enabled` → فعّل Netlify Identity
- `Git Gateway not enabled` → فعّل Git Gateway

---

### 5. اختبار الـ Admin محلياً

**لا يمكن اختبار الـ CMS محلياً بدون Netlify!**

الـ CMS يحتاج:
- Netlify Identity (للمصادقة)
- Git Gateway (للحفظ)

**يجب رفع الموقع على Netlify أولاً**

---

## ✅ كيف تعرف أن الـ Admin يعمل؟

عند فتح `/admin` يجب أن ترى:

1. **شاشة تسجيل الدخول**:
   - زر "Login with Netlify Identity"
   - أو نموذج تسجيل الدخول

2. **بعد تسجيل الدخول**:
   - لوحة تحكم CMS
   - قائمة "Articles & Insights"
   - زر "New Articles & Insights"

---

## 🔘 زر تسجيل الدخول في الـ Navbar

### كيف يعمل:

**عندما لا تكون مسجل دخول:**
- الأيقونة: 🛡️ (Shield)
- عند الضغط: يفتح نافذة تسجيل الدخول

**عندما تكون مسجل دخول:**
- الأيقونة: ✅ (User Check)
- عند الضغط: خيارين
  - OK = الذهاب إلى لوحة التحكم
  - Cancel = تسجيل الخروج

### إذا لم يظهر الزر:

**السبب**: Netlify Identity غير مفعل

**الحل**: فعّل Identity كما في الخطوة 1 أعلاه

---

## 📝 خطوات الاختبار الكاملة

### 1. تفعيل Identity
```
Netlify Dashboard > Site Settings > Identity > Enable Identity
```

### 2. تفعيل Git Gateway
```
Identity Settings > Services > Git Gateway > Enable
```

### 3. دعوة نفسك
```
Identity Tab > Invite users > أدخل إيميلك
```

### 4. افتح الإيميل
- ستصلك دعوة من Netlify
- اضغط "Accept the invite"
- اختر كلمة مرور

### 5. اذهب إلى `/admin`
```
https://your-site.netlify.app/admin
```

### 6. سجل دخول
- استخدم الإيميل وكلمة المرور
- يجب أن تدخل لوحة التحكم

### 7. جرب إنشاء مقال
- اضغط "Articles & Insights"
- اضغط "New Articles & Insights"
- املأ الحقول
- اضغط "Publish"

---

## 🐛 مشاكل شائعة أخرى

### المشكلة: "Config file not found"
**الحل**: تأكد من وجود `admin/config.yml` في المشروع

### المشكلة: "Cannot read posts"
**الحل**: أنشئ مجلد `posts/` في المشروع

### المشكلة: "Cannot upload images"
**الحل**: أنشئ مجلد `assets/images/uploads/`

### المشكلة: "Cannot publish"
**الحل**: 
1. تأكد من تفعيل Git Gateway
2. تأكد من صلاحيات الـ repository

---

## 📞 إذا استمرت المشكلة

1. **تحقق من Netlify Deploy Log**:
   - Netlify Dashboard > Deploys
   - افتح آخر deploy
   - ابحث عن أخطاء

2. **تحقق من Browser Console**:
   - F12 > Console
   - ابحث عن أخطاء باللون الأحمر

3. **جرب في متصفح آخر**:
   - Chrome, Firefox, Edge

---

**ملاحظة مهمة**: الـ CMS **لا يعمل محلياً**. يجب رفع الموقع على Netlify أولاً!
