# خطوات الرفع الصحيحة على Netlify
# Correct Netlify Deployment Steps

---

## 🎯 الطريقة الصحيحة | Correct Method

### الخطوة 1: حذف الموقع الحالي (اختياري)

إذا كان الموقع لا يعمل:
1. Netlify Dashboard > Site settings
2. General > Delete this site
3. أكد الحذف

---

### الخطوة 2: تحضير الملفات

#### افتح مجلد `web v/`

يجب أن ترى:
```
web v/
├── index.html          ✅
├── about.html          ✅
├── contact.html        ✅
├── periods.html        ✅
├── types.html          ✅
├── era.html            ✅
├── 404.html            ✅
├── netlify.toml        ✅ (محدّث!)
├── _redirects          ✅
├── css/                ✅ (مجلد كامل)
├── js/                 ✅ (مجلد كامل)
├── images/             ⚠️ (فارغ - أضف صور)
├── data/               ✅
├── admin/              ✅
├── assets/             ✅
└── posts/              ✅
```

---

### الخطوة 3: الرفع على Netlify

#### الطريقة A: Drag & Drop (الأسهل)

1. **اذهب إلى**: https://app.netlify.com
2. **سجل دخول**
3. **اضغط "Add new site"** > **Deploy manually**
4. **افتح مجلد `web v/`**
5. **اختر جميع الملفات والمجلدات داخله**
   - اضغط `Ctrl + A` لتحديد الكل
6. **اسحبهم** إلى منطقة الرفع في Netlify
7. **انتظر** حتى ينتهي الرفع (1-2 دقيقة)

**مهم جداً:**
- ❌ لا ترفع مجلد `web v/` نفسه
- ✅ ارفع **محتويات** المجلد فقط

---

#### الطريقة B: Git + GitHub (الأفضل)

```bash
# 1. في مجلد المشروع
cd "d:\web v"

# 2. Initialize Git (إذا لم يكن موجود)
git init

# 3. أضف جميع الملفات
git add .

# 4. Commit
git commit -m "Deploy to Netlify"

# 5. أنشئ repository على GitHub
# اذهب إلى github.com > New repository
# اسمه مثلاً: egyptian-heritage

# 6. ربط Git بـ GitHub
git remote add origin https://github.com/username/egyptian-heritage.git
git branch -M main
git push -u origin main

# 7. في Netlify Dashboard
# - New site from Git
# - Connect to GitHub
# - اختر المشروع
# - Build settings:
#   * Build command: (فارغ)
#   * Publish directory: .
# - Deploy site
```

---

### الخطوة 4: إعدادات Netlify

بعد الرفع، تحقق من:

1. **Site settings** > **Build & deploy**
2. **Build settings**:
   - Build command: (فارغ أو `""`)
   - Publish directory: `.` (نقطة)
   - Functions directory: (فارغ)

إذا كانت خاطئة:
- اضغط **Edit settings**
- غيّر Publish directory إلى `.`
- احفظ
- **Deploys** > **Trigger deploy** > **Deploy site**

---

### الخطوة 5: التحقق

#### افتح موقعك

1. انسخ رابط الموقع من Netlify
2. مثال: `https://random-name-123.netlify.app`
3. افتحه في المتصفح

#### اضغط F12

1. **تبويب Console**:
   - يجب ألا يكون هناك أخطاء حمراء
   
2. **تبويب Network**:
   - حدّث الصفحة (F5)
   - ابحث عن `style.css`
   - يجب أن يكون **200** (أخضر)
   - ابحث عن `script.js`
   - يجب أن يكون **200** (أخضر)

3. **تبويب Elements**:
   - ابحث عن `<link rel="stylesheet" href="css/style.css">`
   - يجب أن يكون موجود

---

## 🐛 إذا لم يعمل CSS

### السيناريو 1: 404 Not Found

**المشكلة**: مجلد `css/` لم يُرفع

**الحل**:
1. تأكد أن مجلد `css/` موجود في المشروع
2. احذف الموقع من Netlify
3. ارفع من جديد بالطريقة الصحيحة

---

### السيناريو 2: 200 OK لكن CSS لا يعمل

**المشكلة**: MIME type خاطئ

**الحل**:
1. تأكد أن `netlify.toml` محدّث (تم تحديثه!)
2. **Deploys** > **Trigger deploy** > **Clear cache and deploy site**

---

### السيناريو 3: الموقع يعمل محلياً فقط

**المشكلة**: مسارات محلية

**الحل**:
1. افتح `index.html`
2. تأكد من:
   ```html
   ✅ <link href="css/style.css">
   ❌ <link href="D:/web v/css/style.css">
   ❌ <link href="file:///D:/web v/css/style.css">
   ```

---

## 📸 ماذا تفعل الآن؟

### الخيار 1: جرب الرفع من جديد

1. احذف الموقع من Netlify
2. اتبع **الخطوة 3** أعلاه بدقة
3. تأكد من رفع **محتويات** المجلد وليس المجلد نفسه

### الخيار 2: شارك معلومات أكثر

إذا استمرت المشكلة، شارك:
1. **رابط الموقع على Netlify**
2. **صورة من F12 > Console**
3. **صورة من F12 > Network** (بعد تحديث الصفحة)
4. **صورة من Netlify Deploy Log**

---

## ✅ Checklist السريع

قبل الرفع:
- [ ] مجلد `css/` موجود ✅
- [ ] ملف `css/style.css` موجود ✅
- [ ] مجلد `js/` موجود ✅
- [ ] ملف `js/script.js` موجود ✅
- [ ] `netlify.toml` محدّث ✅
- [ ] جميع المسارات نسبية ✅

أثناء الرفع:
- [ ] رفعت **محتويات** المجلد وليس المجلد نفسه ✅
- [ ] Publish directory = `.` ✅

بعد الرفع:
- [ ] افتحت F12 > Network ✅
- [ ] `style.css` - Status 200 ✅
- [ ] الموقع منسق بشكل صحيح ✅

---

**تم تحديث `netlify.toml` لإصلاح مشكلة CSS!**  
**ارفع الموقع من جديد باستخدام الخطوات أعلاه.**
